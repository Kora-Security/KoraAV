//placeholder//
(This file only exists to keep the github repo folder path existing. So if I delete all of the yara rule files, the whole file path/directory won't get deleted as well.)
(This file can be deleted, it's just for github path sake.)
